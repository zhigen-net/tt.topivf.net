FROM node:22-alpine
WORKDIR /app
RUN npm install -g pnpm
COPY package.json pnpm-lock.yaml* ./
RUN pnpm install --ignore-scripts 2>/dev/null || npm install
COPY . .
EXPOSE 3001
CMD ["npm", "run", "start:dev"]
